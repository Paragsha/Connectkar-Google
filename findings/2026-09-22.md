# ConnectKar Repository Audit Findings — 2026-09-22

Audit executed per ConnectKar Audit Rules checklist.

---

## CRITICAL

- `app/src/main/java/com/connectkar/auth/AuthViewModel.kt:159`: `firebaseAuth == null` fallback branch authenticates resident with simulated mock credentials without checking `if (BuildConfig.DEBUG)`.
- `app/src/main/java/com/connectkar/ui/DashboardScreen.kt:1084`: `text = "simulated_proof_of_residence.pdf"` is hardcoded in the resident profile dialog and displayed unconditionally in release builds rather than referencing `currentUser.proofDocumentUri` or being gated behind `BuildConfig.DEBUG`.

*(Note: Architecture guardrails verified 0 hits for DI frameworks, 0 hits for Gson, and 0 hits for Type-safe Compose routes. Schema version 11 has exactly 10 unbroken migrations `MIGRATION_1_2` through `MIGRATION_10_11`. No hardcoded secrets detected).*

---

## HIGH

- `app/src/main/java/com/connectkar/ConnectKarApplication.kt:27`: (Informational) `fallbackToDestructiveMigration()` is NOT present on the `Room.databaseBuilder()` instance (safe); companion object in `AppDatabase.kt` defines all 10 migrations.
- `app/src/main/java/com/connectkar/data/repository/SyncWorker.kt:1`: Unintended periodic sync check: 0 occurrences of `PeriodicWorkRequest` found. Sync remains strictly one-shot via `OneTimeWorkRequest`.

---

## MEDIUM

- `app/src/main/java/com/connectkar/ui/TownshipViewModel.kt:186`: `rentalsRecentListings` maps over `exploreListings` with in-memory filter (`it.type == "PROPERTY" || it.type == "VEHICLE" || it.type == "HOUSEHOLD_ITEM"`) and in-memory sort/take (`.sortedByDescending { it.timestamp }.take(10)`) rather than querying via Room DAO.
- `app/src/main/java/com/connectkar/ui/TownshipViewModel.kt:177`: `rentalsCategoryCounts` counts distinct rental types via in-memory filter over `exploreListings` rather than a `COUNT(...) GROUP BY type` Room query.

---

## LOW / Informational

- `app/src/main/java/com/connectkar/ui/TownshipViewModel.kt:430`: `triggerSync()` call site — delegates to `repository.triggerSync()`; verified absent from all `init {}` blocks and `MainActivity.onCreate()`.
- `app/src/main/java/com/connectkar/data/repository/TownshipRepository.kt:83`: `triggerSync()` call site — schedules one-time work request via `scheduleSyncJob()`.
- `firestore.rules:129`: `match /reviews/{reviewId}` defines security rules for `/listings/{listingId}/reviews/{reviewId}` subcollection, but no client-side read/write code references this subcollection.
- `app/src/main/java/com/connectkar/data/FirebaseManager.kt:86`: `FirebaseFunctions.getInstance()` property and `libs.firebase.functions` dependency (`app/build.gradle.kts:61`) are unused by client code (Cloud Functions in `functions/index.js` use background Firestore triggers).
- `gradle/libs.versions.toml:69`: [Lockfile/Dependency Drift] Added KSP plugin (`version = "2.3.5"`) and `androidx-room-compiler` to support Room annotation processing in test/build environments.
- `app/src/main/java/com/connectkar/ui/theme/Color.kt:1`: [Design Tokens] Token sources intact (`Color.kt` and `Components.kt`). No new hex tokens introduced in `PublishSuccessSheet.kt`.
- TODO/FIXME density: 0 total occurrences across codebase (delta: 0).
